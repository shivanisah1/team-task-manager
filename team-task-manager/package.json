{
  "name": "team-task-manager",
  "version": "1.0.0",
  "scripts": {
    "build": "npm --prefix frontend install && npm --prefix backend install && npm --prefix frontend run build",
    "start": "npm --prefix backend start"
  }
}
